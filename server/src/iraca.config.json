{
  "predictionEditingWindow": "${SKO_PREDICTION_EDITING_WINDOW}",
  "database!": {
    "host": "${SKO_DB_HOST}",
    "port": "${SKO_DB_PORT}",
    "username": "${SKO_DB_USER}",
    "password": "${SKO_DB_PASSWORD}",
    "name": "${SKO_DB_NAME}",
    "logging": "${SKO_DB_LOGGING}"
  },
  "eventBus!": {
    "eventBusName": "${SKO_EVENT_BUS_NAME}",
    "source": "${SKO_EVENT_BUS_SOURCE}"
  }
}
