{
  "name": "@skorify/server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "build:deps": "pnpm --dir ../domain install --frozen-lockfile && pnpm --dir ../shared install --frozen-lockfile && pnpm --dir ../domain build && pnpm --dir ../shared build",
    "fullstart:dev": "export SKO_PREDICTION_EDITING_WINDOW=${SKO_PREDICTION_EDITING_WINDOW:-60} && pnpm run build:deps && nodemon",
    "start:dev": "nodemon",
    "build": "tsc -p tsconfig.json && pnpm run copy-iraca && pnpm run copy-auth && pnpm run copy-libs && pnpm run copy-npmrc && pnpm run copy-package",
    "copy-iraca": "copyfiles  src/iraca.config.json dist",
    "copy-auth": "copyfiles -u 1 auth/**/* dist/auth",
    "copy-libs": "copyfiles -u 1 libs/**/* dist/libs",
    "copy-npmrc": "copyfiles .npmrc dist",
    "copy-package": "copyfiles package.json dist",
    "start:heroku": "node dist/src/index.js"
  },
  "dependencies": {
    "@scifamek-open-source/iraca": "9.2.0",
    "@scifamek-open-source/logger": "1.1.2",
    "@skorify/data": "github:aws-ug-manizales/Skorify_Data#v1.2.1-beta.5",
    "@skorify/domain": "../domain/dist",
    "@skorify/shared": "../shared/dist"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "22.5.0",
    "copyfiles": "^2.4.1",
    "jest": "^30.3.0",
    "nodemon": "3.1.10",
    "ts-jest": "^29.4.9",
    "ts-node": "10.9.2",
    "typescript": "5.8.3"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.22.0"
}
